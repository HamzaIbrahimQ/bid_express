import 'package:flutter/material.dart';

class SellerDecisionPage extends StatelessWidget {
  const SellerDecisionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
